Python的整数没有大小限制，而某些语言的整数根据其存储长度是有大小限制的，Python的浮点数也没有大小限制，但是超出一定范围就直接表示为inf（无限大）。
数据类型
	整数
		Python可以处理任一大小的整数，包括负整数
	浮点数
		科学计数法表示 10用e代替，整数和浮点数在计算机内部存储的方式不一样，整数运算永远是精确的，浮点运算可能是四舍五入的误差
	字符串
		可以用''或"".如果字符串中包含'，''，可以使用转义字符\来标识，若有很多需要转义，为了简化还可以使用r''表示''内部的字符串默认不转义
		如 字符串内部有很多换行，用\n写在一行里不好读，为了简化，使用'''...'''格式表示多行内容
		'print('I\'m \"OK\".')	
		print(r'\\\t\\')
	布尔值
		布尔值和布尔代数的表示完全一致，一个布尔值只有True、False两种值，要么是True，要么是False，在Python中，可以直接用True、False表示布尔值（请注意大小写），也可以通过布尔运算计算出来
		布尔值可以用and、or和not运算。
	空值
		空值是Python里一个特殊的值，用None表示。None不能理解为0，因为0是有意义的，而None是一个特殊的空值。
	list
		list是一种有序的集合，可以随时添加、删除其中的元素。
	tuple
		tuple是一个不可变的有序集合，一旦定义就不可以更改指向
	dic
		dic是一个无序键值对集合
	set
		set是一个无序，无重复键集合
变量
	等号=是赋值语句，同一个变量可以反复赋值，而且可以是不同类型的变量,这种变量类型本身不固定的语言称为动态语言,
	与之对应的是静态语言，静态语言在定义时必须指定变量类型，如果赋值的时候类型不匹配，就会报错的

	a="123";
	print(a);
	a="abc";
	print(a);
	上述中，如a="123" Python解释器干了两件事，
	第一件是在内存中创建一个"123"的字符串.
	第二件是在内存中创建一个名为a的变量，并把它指向"123".
常量
	常量通常全部大写表示
	除法：
		/ --结算结果是浮点数，
		//--地板除法，得到的是整数部分。
		%--求余数
	print("/除法：",10/3,"  //除法=",10//3,"  %求余数=",10%3)